switch("c")
switch("nimcache", "c")

switch("noMain")

switch("os", "standalone")
switch("exceptions", "setjmp")
switch("d", "nimNoLibc")

switch("checks", "off")
switch("assertions", "off")
switch("stackTrace", "off")

switch("lib", "NimLibs")
