switch("c")
switch("nimcache", "c")

switch("noMain")

switch("os", "any")
switch("gc", "orc")
switch("d", "useMalloc")
switch("d", "noSignalHandler")

switch("checks", "off")
switch("assertions", "off")
switch("stackTrace", "off")

switch("lib", "NimLibs")
