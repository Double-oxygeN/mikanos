switch("c")
switch("nimcache", "c")

switch("noMain")

switch("os", "any")
switch("d", "standaloneHeapSize=33554432")
switch("gc", "orc")
switch("d", "nimNoLibc")

switch("checks", "off")
switch("assertions", "off")
switch("stackTrace", "off")

switch("lib", "NimLibs")
